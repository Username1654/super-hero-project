<!-- views/heroList.ejs -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>super hero List</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <h1><i class="bi bi-emoji-sunglasses-fill"></i>
        hero list</h1>
        <%const superHeroes = {Superman = {superName: "SuperMan",realName: "Clark Kent",superPower: "Super strength, flying, lazer eyes, super breath, super vision and super hearing.",powerLevel: "10",secredIdentity: "yes",}}%>
       <% if (superHeroes &&superHeroes.length > 0) { %>
      <div class="row">
        <%superHeroes.forEach(function(hero) { %>
        <div class="col-md-6 col-lg-4 mb-3" id="hero-<%= hero.realName %>">
          <div class="card">
            <div class="card-body">
              <div class="hero-display" id="display-<%= hero.realName %>">
                <h5 class="card-title">
                    <i class="bi bi-person"></i></i>
                  <%= hero.superName %>
                </h5>
                <!-- <% if (hero.realName) { %>
                <div class="d-flex gap-2">
                  <button
                    class="btn btn-sm btn-outline-primary"
                    onclick="edithero('<%= hero._id %>', '<%= hero.fruitName %>')"
                  >
                    <i class="bi bi-pencil me-1"></i>Edit
                  </button>
                  <button
                    class="btn btn-sm btn-outline-danger"
                    onclick="deletehero('<%= hero._id %>')"
                  >
                    <i class="bi bi-trash me-1"></i>Delete
                  </button>
                </div>
                <% } %> -->
              </div>
              <!-- <div
                class="hero-edit"
                id="edit-<%= hero._id %>"
                style="display: none"
              >
                <div class="mb-3">
                  <input
                    type="text"
                    class="form-control"
                    id="fruitName-<%= hero._id %>"
                    value="<%= hero.fruitName %>"
                    placeholder="Fruit Name"
                  />
                </div>
                <div class="d-flex gap-2">
                  <button
                    class="btn btn-sm btn-success"
                    onclick="updatehero('<%= hero._id %>')"
                  >
                    <i class="bi bi-check me-1"></i>Save
                  </button>
                  <button
                    class="btn btn-sm btn-secondary"
                    onclick="cancelEdit('<%= hero._id %>')"
                  >
                    <i class="bi bi-x me-1"></i>Cancel
                  </button>
                </div>
              </div> -->
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } else { %>
      <div class="alert alert-info text-center">
        <i class="bi bi-info-circle me-2"></i>
        No heros found
      </div>
      <% } %>
      <div class="text-center mt-4">
        <a href="/" class="btn btn-primary">
          <i class="bi bi-plus-circle me-2"></i>
          Add New hero Item
        </a>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/heroActions.js"></script>
  </body>
</html>
